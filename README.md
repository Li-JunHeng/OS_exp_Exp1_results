# Exp1 Results

This repository is mounted as the `results/` submodule of the Exp1 CPU project.

Use it for generated reports, screenshots, bitstream notes, and final validation artifacts that should be versioned separately from the source project.
