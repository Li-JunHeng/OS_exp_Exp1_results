# Bitstream Output

`scripts/build_bitstream.tcl` copies the generated bitstream here as:

```text
OS_Exp_1_top.bit
```

Do not commit generated `.bit` files unless a specific submission requires it.
